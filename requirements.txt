# 基础安装（CLI 模式，无 GUI/API 可选依赖）
# GUI 桌面应用:  uv pip install -e .[gui]
# REST API 服务: uv pip install -e .[serve]
# 全部功能:      uv pip install -e .[all]
Pillow>=10.0.0
jinja2>=3.1.0
uv>=0.4.0
requests>=2.31.0
keyring>=24.0.0
click>=8.0
typer>=0.12
rich>=13.0
